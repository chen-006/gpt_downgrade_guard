# 实现状态

更新时间：2026-08-31

## 当前状态

- [x] 读取分组 A、分组 B 中的 OpenAI 账号
- [x] 只探测 `group_ids` 包含 A 或 B 的账号
- [x] 每个账号执行 9 条指纹探针
- [x] 所有账号并行探测，单账号最多 3 并发
- [x] 每条探针请求保留初次请求加 4 次重试预算
- [x] 输出 Sol、Terra、Luna 三模型指纹匹配度
- [x] 请求未全部完成时显示“网络错误/上游错误”且不移动分组
- [x] 根据严格或宽松规则在 A、B 之间移动账号
- [x] 保留最近 100 条历史，最新记录显示在最左侧
- [x] 配置自动保存，页面轮询不覆盖编辑内容、不改变滚动位置
- [x] Chromium 浏览器管理员令牌自动获取和本地验证
- [x] 管理员令牌仅保存在进程内存，不写入配置文件
- [x] 黑色本地管理页面
- [x] Windows 一键启动脚本
- [x] Python 与 JavaScript 语法检查
- [x] 使用真实 Sub2API 账号完成 9/9 探针、评分和页面显示验收
- [x] 补丁脚本已在当前官方 Sub2API `main` 源码上完成首次应用验证

## 注意

- 当前仍是测试版。
- `install/apply_sub2api_patch.ps1` 只应执行一次；已经安装补丁时不要重复执行。
- 需要 Python 3.10 或更高版本。
- 管理员令牌不会持久化，程序重启后需要重新自动获取或手动填写。
- 本文件不记录令牌、账号凭据或其他秘密。
